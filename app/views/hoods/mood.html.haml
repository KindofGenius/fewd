%h1.text-center=@mood.name
-@dishes.each do |dish|
  .col-sm-12.col-md-6
    =link_to dish_path(dish) do
      .dish-box
        =image_tag(dish.dish_images.first.avatar.url, class: "img-responsive")
        .text-center=dish.name